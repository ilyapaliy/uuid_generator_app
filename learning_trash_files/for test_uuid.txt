# from httpx import AsyncClient
from src.consumer import consumer_main
import logging
import subprocess
# import signal
# from time import time
# from datetime import datetime

import os, errno
# os.remove((os.getcwd() + "/app.log"))


def test_consumer(caplog):
    assert False
    # print("##########  BEFORE  ###########")
    # cmd = "python " + os.getcwd() + "/src/consumer.py"
    # subprocess.run(cmd, shell=True, universal_newlines=True, check=True)
    # caplog.set_level(logging.INFO)
    # print(caplog.record_tuples)
    # print(caplog.messages)
    # assert caplog.messages == [1, 3]
    # print("##########  AFTER  ###########")
    
    # with capsys.disabled():
    # results = subprocess.run(
    # cmd, shell=True, universal_newlines=True, check=True)
    # print(results.stdout)
    # print(caplog.record_tuples)
    # with capsys.disabled():
    # consumer_main(1)
    # captured = capsys.readouterr()
    # print(captured)
    # caplog.set_level(logging.CRITICAL)
    # consumer_main(1)

# def test_is_green_red_logged():
#     consumer_main()
#     with open(os.getcwd() + "/src/app.log") as file:
#         lines = [line.rstrip() for line in file]
#         re_log_line = re.compile('^' + asctime_pattern + ' - INFO - UUID: ' + uuid_pattern + ' - X-Flag: red$', re.I)
#         print("####################################")
#         print(lines[-1])
#         print("####################################")
#         assert re_log_line.match(lines[-1])
#         x_flag = re_log_line.match(lines[-1]).group(0)[-3:]
#         # print(x_flag)
#         assert x_flag == "red"




# def test_uuid():
#     assert 1 == 1

# async def test_register(ac: AsyncClient):
#     response = await ac.post('/register/', json={
#         "firstname": "Firstname",
#         "lastname": "Lastname",
#         "email": "flastname@example.com",
#         "password": "password"
#     })

#     assert response.status_code == 201, 'User was not created'


# async def test_get_token(ac: AsyncClient):
#     response = await ac.post('/token/', json={
#         "email": "flastname@example.com",
#         "password": "password"
#     })

#     assert response.status_code == 200, 'Incorrect status on login operation'
#     assert response.json().get('access_token', None), 'Token has not bee provided'
